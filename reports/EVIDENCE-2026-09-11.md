# Tier 1 Evidence — 2026-09-11

Scope: `https://eastlakeview503.relahq.com` (Riva Condominium unit #503, 1550 Eastlake Ave E,
Seattle WA 98102, MLS# 2556157, list price $649,888, built 1997). Tier 1 is a factual record
only — no recommendations or priority calls here; that is Tier 2's job.

## 0. Infrastructure failure this run (read first)

**All outbound fetches to external HTTP(S) hosts failed in this session**, including a plain
control fetch of `en.wikipedia.org` used to isolate the cause. Both fetch paths were tried:

- `curl` (direct, via the environment's egress proxy) → `CONNECT tunnel failed, response 403`
  for `eastlakeview503.relahq.com:443`, logged by the proxy as `connect_rejected` /
  "gateway answered 403 to CONNECT (policy denial or upstream failure)".
- `WebFetch` tool → `{"error_type":"EGRESS_BLOCKED", ...}` for every domain tried:
  `eastlakeview503.relahq.com` (root, `/llms.txt`, `/sitemap.xml`), `pagespeed.web.dev`,
  `www.rsir.com`, `www.windermere.com`, `www.compass.com`, `seattlecondosandlofts.com`,
  `houseboatsofseattle.com`, and the control domain `en.wikipedia.org`.

Since even Wikipedia was blocked, this is an environment/network-policy condition for this
session, not a Rela-side or target-site-specific block. Only the `WebSearch` tool returned
results this run — it appears to run through a different backend than the egress proxy.

**Practical effect:** Steps 2 (live page HTML/meta/JSON-LD/images/GA tag), 4 (competitor
deep-fetch of page-level HTML), and 5 (PageSpeed/Core Web Vitals) could not be executed this
run. Only step 3 (search-query snapshots, via `WebSearch`) produced data. Per instructions,
each source was retried at most once before being logged as failed and moved past.

## 1. Live site fetch — `https://eastlakeview503.relahq.com`

**Status: FAILED — EGRESS_BLOCKED (see §0). No HTTP status, headers, title, meta tags,
JSON-LD, H1, image/alt counts, or GA tag presence could be recorded this run.**

- `GET /` → egress blocked (both curl and WebFetch)
- `GET /llms.txt` → egress blocked (WebFetch)
- `GET /sitemap.xml` → egress blocked (WebFetch)

None of these were retried a second time, per instructions.

## 2. Tracked query snapshots (via WebSearch)

Caveat: `WebSearch` returns a synthesized top-result list (this run: 9–10 links per query),
not a guaranteed ordered top-20 SERP, so "does not appear in top 20" below means "did not
appear among the results WebSearch returned," not a confirmed rank-21+ result.

### "1550 Eastlake Ave E #503"
Top domains returned: redfin.com (x3, incl. the unit-503 page), zillow.com, apartments.com
(x2), movoto.com.
`eastlakeview503.relahq.com` present: **No.**

### "Riva Condos #503 Seattle"
Top domains returned: apartments.com, redfin.com, apartmentfinder.com, apartments.com
(riva-townhomes-issaquah, different property), wicklundre.com, facebook.com, yelp.com,
southernresorts.com (unrelated "Bella Riva" in FL), urbnlivn.com (different Riva in
Issaquah), dwellresidential.com.
`eastlakeview503.relahq.com` present: **No.**

### "Riva Condominium Seattle for sale"
Top domains returned: wicklundre.com (x2, one is a different "Riva Chase" in Bellevue),
apartments.com, redfin.com, yelp.com, zillow.com, estately.com, coldwellbankerhomes.com,
homes.com, livabl.com.
`eastlakeview503.relahq.com` present: **No.**

### "Eastlake condo for sale Lake Union view"
Top domains returned: homes.com, redfin.com (x2), zillow.com, wicklundre.com,
seattlearearealestateteam.com, homes77.com, expedia.com (unrelated hotel listing).
`eastlakeview503.relahq.com` present: **No.**

### "eastlakeview503"
Top domains returned: wikipedia.org (x2, unrelated "Eastlake"/"Eastview" articles),
x.com (East Lakeview Neighbors, Chicago — unrelated), eastlakeview.org (unrelated Chicago
neighborhood group), eastlakeview.com (unrelated), bjbproperties.com (unrelated).
`eastlakeview503.relahq.com` present: **No.** The bare domain token returns zero related
results; nothing in the returned set is about this listing.

## 3. Competitor deep-fetch (state/competitors.json → deep_fetch)

All five target domains were attempted via `WebFetch` and all failed with `EGRESS_BLOCKED`
(see §0), so no title/meta/JSON-LD comparison could be recorded this run. For record-keeping,
the specific candidate URLs located via `WebSearch` (which would have been the fetch targets)
were:

- **seattlecondosandlofts.com** — no dedicated Riva/1550 Eastlake page found via search; the
  building appears only inside a "Pet Friendly Seattle Condos" PDF. Candidate checked:
  `seattlecondosandlofts.com/list-of-condoslofts` (fetch blocked).
- **houseboatsofseattle.com** — no Riva/1550 Eastlake page found via search; the site's
  Eastlake-area content centers on a different building ("Union Harbor Condominium").
  Candidate checked: `houseboatsofseattle.com/wa/seattle/eastlake/` (fetch blocked).
- **rsir.com** — candidate found: `www.rsir.com/homes-for-sale/1550-eastlake-avenue-e-201-seattle-wa-98102-2402758-1785003/`
  (unit #201 MLS listing, not #503) (fetch blocked).
- **windermere.com** — candidates found: listings for unit #506 and unit #405 at the same
  address, not #503. Checked: `www.windermere.com/listing/WA/Seattle/1550-Eastlake-Avenue-E-506-98102/197478285`
  (fetch blocked).
- **compass.com** — candidates found: listings for units #203, #405, #202 at the same
  address, not #503. Checked: `www.compass.com/listing/1550-eastlake-avenue-east-unit-203-seattle-wa-98102/1561675205357517033/`
  (fetch blocked).

## 4. Core Web Vitals / PageSpeed

**Status: unavailable this run.** `pagespeed.web.dev` fetch failed with `EGRESS_BLOCKED`
(see §0). Mobile LCP/INP/CLS not obtained.

## 5. Deltas vs last snapshot

`state/last-snapshot.json` currently contains only `{"schema_version":1,"first_run":null,
"note":"Tier 1 populates this on first run."}` — there is no prior structured snapshot to diff
against, and this file is Tier 2's to populate/own, so it was not modified here.

For context only (not a formal delta, since it comes from `README.md`'s documented baseline
rather than `last-snapshot.json`): the README records a 2026-09-10 baseline (title, meta
description, og:title, JSON-LD yearBuilt/price/description wording) captured manually via
Chrome in the Rela editor, plus a list of pending Rela-platform fixes
(`twitter:card`, `og:url`, `og:locale`, `twitter:image:alt`, H1 text, JSON-LD
`numberOfBedrooms`/`amenityFeature`/`petsAllowed`/`containedInPlace`, `<img loading="lazy">`,
`/sitemap.xml`, `robots.txt` `Crawl-delay`/`Sitemap:` line, `/llms.txt`). None of this could be
re-verified live this run because of the fetch outage in §0 — Tier 2 should treat the
on-page/technical-SEO state as **unverified this cycle**, not as "unchanged."

The only new information this run relative to that baseline is the search-visibility snapshot
in §2, which shows no appearance of `eastlakeview503.relahq.com` across five tracked queries.
Whether that is a change from prior weeks cannot be determined, since no prior search-snapshot
data exists in `state/last-snapshot.json`.
