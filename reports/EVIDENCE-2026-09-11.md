---
run_date: 2026-09-11
status: complete
fields_captured: 18/19
fetch_failures: rsir.com (Cloudflare JS challenge, confirmed non-fetchable, not retried per policy), PageSpeed API (429 quota exceeded, not retried per policy)
listing_status: active
indexed: no
---

# Tier 1 Evidence — 2026-09-11

Scope: `https://eastlakeview503.relahq.com` (Riva Condominium unit #503, 1550 Eastlake Ave E,
Seattle WA 98102, MLS# 2556157, list price $649,888, built 1997). Factual record only — no
recommendations or priority calls here; that is Tier 2's job.

## Methodology note

This run supersedes the same-day report body previously at this path (three ad-hoc check-ins
this morning, commits `55ce115`, `be20aae`, `f9b2605`, plus a contract-hardening commit
`d49247b` that defines the header block this file now starts with). All data below was captured
fresh this run via `curl` with a browser User-Agent, then parsed with exact regex/`python3
json.loads` extraction — no LLM-summarized tool output was used for fact extraction.

One git-hygiene note, not a site finding: at the start of this run, `main`/`origin/main` were
pinned at the *first* of today's three prior commits (`55ce115`); the other three commits
existed only on a detached `HEAD` and had never reached `main`. Fast-forwarded `main` to include
them (linear history, no conflicts, nothing discarded) and pushed before starting this run's
work, so this run's diff is built on top of the fully hardened contract.

## LISTING STATUS CHECK

No change. `offers.availability` = `https://schema.org/InStock`, `offers.price` = `649888`
(matches the $649,888 list price in scope above). `state/last-snapshot.json` is still the
unpopulated placeholder (`{"schema_version":1,"first_run":null}` — Tier 2 has not yet written a
baseline), so no mechanical price diff was possible against it; comparison here is against the
list price stated in this task's scope, which is unchanged. Page body shows no Sold/Pending/Under
Contract indicator. **Status: active, unchanged.**

## INDEXATION CHECK

`WebSearch` for `site:eastlakeview503.relahq.com` returned **zero pages from the target domain**
— all 10 results returned are unrelated Rela corporate/marketing pages and other unrelated Rela
property subdomains (`8101connecticutavec603.relahq.com`, `100146thstreet.relahq.com`,
`4425dianedr.relahq.com`), none of them `eastlakeview503.relahq.com`. **This target page does
not appear to be indexed under a site: query.** (Caveat, as in prior runs: `WebSearch` returns a
synthesized result list, not a certified raw Google index dump — but a `site:` query returning
zero matches for the exact domain is a strong negative signal regardless.)

## 1. Live site fetch — `https://eastlakeview503.relahq.com`

- **HTTP status:** 200 (`curl` + browser UA, `HTTP/2 200`, `server: nginx`, `via: varnish, ...
  (CloudFront)`, `x-cache: Miss from cloudfront`).
- **Final URL:** `https://eastlakeview503.relahq.com/` (no redirect).
- **`<title>`:** `Riva Condos #503, 1550 Eastlake Ave E – Seattle Condo for Sale`
- **`<meta name="description">`:** `Top-floor 2 bed, 2 bath condo for sale at Riva Condominium,
  1550 Eastlake Ave E #503, Seattle. 1,114 sq ft, Lake Union & skyline views, private balcony, 2
  secured parking spaces + storage. $649,888 · MLS# 2556157.`
- **`<link rel="canonical">`:** `https://eastlakeview503.relahq.com`
- **`<meta name="robots">`:** not present.
- **`<meta name="google-site-verification">`:** `T6_j8jTeByDHz6TZ-fo1x88Et-9sj4_CwAIZFHl_2yM`

### og:* / twitter:* tags (verbatim, present tags only)

| Tag | Value |
|---|---|
| `og:type` | `place` |
| `og:title` | `Riva Condos #503 – Top-Floor Eastlake Condo with Lake Union Views` |
| `og:description` | `Move-in-ready 2 bed / 2 bath top-floor condo for sale at Riva Condominium in Seattle's Eastlake neighborhood, one block from Lake Union and South Lake Union. Private balcony with lake and skyline views, 2 secured parking spaces, dedicated storage. Offered at $649,888 (MLS# 2556157).` |
| `og:updated_time` | `2026-09-10T08:07:16-07:00` |
| `og:image` / `og:image:url` / `og:image:secure_url` | `https://media.relahq.com/public/styles/kb_full/s3/property-images/prop-nid-257763116/hailun-deck-wine_glass-1_697.jpg?v=1789052836` |
| `og:image:type` | `image/jpeg` |
| `og:image:width` / `og:image:height` | `1600` / `1069` |
| `twitter:title` | `Riva Condos #503 – Top-Floor Eastlake Condo, Lake Union Views` |
| `twitter:description` | `2 bed / 2 bath top-floor condo for sale at Riva Condominium, 1550 Eastlake Ave E #503, Seattle. 1,114 sq ft, Lake Union & skyline views, balcony, 2 parking + storage. $649,888 · MLS# 2556157.` |
| `twitter:image` | same media URL as `og:image` |
| `twitter:image:alt` | `1550 Eastlake Ave E Apt 503, Seattle, WA \| 2 Bed, 2 Bath` |

**Not present:** `og:url`, `og:locale`, `twitter:card`.

### JSON-LD (2 blocks)

**Block 1 — static `<script type="application/ld+json">` in raw HTML**, `@graph` with one node:

- `@type`: `Apartment`, `@id`: `https://eastlakeview503.relahq.com#listing`
- `name`: `1550 Eastlake Ave E #503, Seattle, WA 98102`
- `yearBuilt`: `1997`
- `numberOfRooms`: `2` (**no `numberOfBedrooms` field**)
- `numberOfBathroomsTotal`: `2`
- `floorSize`: `1114` sq ft (`unitCode: FTK`)
- `offers`: `{@type: Offer, price: 649888, priceCurrency: USD, availability: InStock, url: <canonical>}`
- `provider`: `{@type: RealEstateAgent, name: "Omar Senghor", email: "change-re@outlook.com"}`
  (no phone/brokerage/headshot fields)
- `address`: 1550 Eastlake Ave E Apt 503, Seattle, WA 98102, US; `geo`: 47.633631, -122.325289
- `image`: 18 URLs
- Full key list on this node: `@type, @id, url, name, description, address, geo, numberOfRooms,
  numberOfBathroomsTotal, floorSize, yearBuilt, image, offers, provider`.
  **No `amenityFeature`, `petsAllowed`, or `containedInPlace` key on this node.**

**Block 2 — client-side JS-injected** (materializes via `l.text = '{...}'` inside an inline
`<script>` IIFE; extracted here by regex on `l.text`, not by parsing `<script>` tags):

- `@type`: `ApartmentComplex`, `name`: `Riva Condominium`
- `alternateName`: `["Riva Condos", "Riva at Lake Union"]`
- `yearBuilt`: `"1997"`, `numberOfFloors`: `5`
- `address`: 1550 Eastlake Ave E, Seattle, WA 98102, US; `geo`: 47.633631, -122.325289 (same as Block 1)
- `amenityFeature` (4): `Secured garage parking`, `Secured lobby entrance`, `Private storage
  units`, `In-unit washer and dryer`
- **No `containedInPlace` link** connecting this node to the Block 1 `Apartment` node in either
  direction.

### H1

Full extracted text: `1550 Eastlake Ave E Apt 503, Seattle` (nested `<span class="thoroughfare">`
/ `<span class="locality">` spans inside an `<h1>`). Uses "Apt 503" rather than "#503", and omits
", WA 98102".

### Images

- `<img>` count: **23** total.
- Property-photo `<img>` tags (`media.relahq.com` property-images path): **19**.
- Logo/chrome `<img>` tags: **4**.
- Non-empty `alt`: **3** total — all three on logo images (`alt="Logo"` x2, `alt="Rela Logo"`
  x1). **0 of the 19 property-photo `<img>` tags have alt text.**
- `loading="lazy"` occurrences: **0**.

### Analytics

- `gtag` token occurrences in page source: 10 (raw grep count, not distinct calls).
- GA Measurement IDs found: `G-1RVXDXERNN` (Rela's own tag, x4 occurrences) and `G-95JF0FLSZH`
  (custom Measurement ID added via Rela's Custom Script field, per `ACTION-QUEUE.md`, x2
  occurrences). **Both present and loading.**

### `/llms.txt`

`GET /llms.txt` → HTTP 200, body confirmed (via `<title>` match) to be the same Drupal SPA page
as `/` — a soft-404/catch-all route, not real `llms.txt` content. Still unpopulated.

### `/sitemap.xml`

`GET /sitemap.xml` → HTTP **404** (`content-length: 0`, `x-cache: Error from cloudfront`).

### `/robots.txt`

`GET /robots.txt` → HTTP 200. Standard Drupal boilerplate. Confirmed still present: `Crawl-delay:
10`. Confirmed still absent: any `Sitemap:` line.

## 2. Tracked query snapshots (via WebSearch)

Caveat: `WebSearch` returns a synthesized result list (~9-10 links per query here), not a
guaranteed ordered top-20 SERP, so "does not appear" means "did not appear among the results
`WebSearch` returned."

### "1550 Eastlake Ave E #503"
Top domains: redfin.com (x3, incl. the unit-503 page and unit-303), zillow.com, loopnet.com,
apartments.com (x2), movoto.com, westlakeassociates.com (PDF flyer, different unit — retail
space for lease at same address).
`eastlakeview503.relahq.com` present: **No.**

### "Riva Condos #503 Seattle"
Top domains: apartments.com (x2, one "Riva Townhomes" in Issaquah — different property),
redfin.com, apartmentfinder.com, wicklundre.com, facebook.com, yelp.com, southernresorts.com
(unrelated "Bella Riva" FL), urbnlivn.com (different Riva, Issaquah), dwellresidential.com.
`eastlakeview503.relahq.com` present: **No.**

### "Riva Condominium Seattle for sale"
Top domains: wicklundre.com (x2, one is "Riva Chase" in Bellevue — different property),
apartments.com, redfin.com, yelp.com, zillow.com, estately.com, coldwellbankerhomes.com,
homes.com, livabl.com.
`eastlakeview503.relahq.com` present: **No.**

### "Eastlake condo for sale Lake Union view"
Top domains: homes.com, redfin.com (x3), zillow.com, wicklundre.com,
seattlearearealestateteam.com, homes77.com, expedia.com (unrelated hotel listing).
`eastlakeview503.relahq.com` present: **No.**

### "eastlakeview503"
Top domains: wikipedia.org (x2, unrelated "Eastlake"/"Eastview" articles), x.com (East Lakeview
Neighbors, Chicago — unrelated), eastlakeview.org / eastlakeview.com (unrelated Chicago
neighborhood group/site), bjbproperties.com (unrelated).
`eastlakeview503.relahq.com` present: **No.** The bare domain token returns zero related results.

## 3. Competitor deep-fetch (state/competitors.json → deep_fetch)

- **rsir.com** — marked `fetchable: false` in `competitors.json`. Confirmed still blocked this
  run: `403`, `cf-mitigated: challenge` (genuine Cloudflare Turnstile/JS challenge, not fetchable
  by curl with any UA). **Not retried, per policy.** No title/meta/JSON-LD recorded.
- **windermere.com** — `https://www.windermere.com/listing/WA/Seattle/1550-Eastlake-Avenue-E-506-98102/197478285`
  (unit #506, not #503) → 200. `<title>`: `MLS#2287238: 1550 Eastlake Avenue E 506, Seattle, WA
  98102`. Meta description: 1-bed/1-bath unit copy, list price visible in body: `$385,000`.
  JSON-LD `@type`s present: `RealEstateListing`, `House`, `PostalAddress`, `ImageObject` (x21),
  `QuantitativeValue`, `Product`, `Offer`.
- **compass.com** — `https://www.compass.com/listing/1550-eastlake-avenue-east-unit-203-seattle-wa-98102/1561675205357517033/`
  (redirected to `.../homedetails/1550-Eastlake-Ave-E-Unit-203-Seattle-WA-98102/1561675205357517033_lid/`;
  unit #203, not #503) → 200. `<title>`: `1550 Eastlake Ave, Unit 203, Seattle, WA 98102 | MLS
  #2227901`. Meta description: `"...is a property not currently listed. This is a 2-bed, 2-bath
  property."` (i.e. this comp listing itself reads as off-market). JSON-LD `@type`s present:
  `SingleFamilyResidence`, `RealEstateListing`, `Product`, `Brand`, `Place`, `PostalAddress` (x2),
  `ImageObject`, `Offer`, `Organization`, `ContactPoint`, `FloorPlan`, `BreadcrumbList`,
  `ListItem` (x7), `Thing` (x6).
- **seattlecondosandlofts.com** — `https://seattlecondosandlofts.com/list-of-condoslofts` (no
  dedicated Riva/#503 page; general condo directory) → 200. `<title>`: `Condo Directory`. Meta
  description generic, references buildings "updated 2015" — not Riva-specific, no list price.
  JSON-LD `@type`s present: `BreadcrumbList`, `ListItem` (x2), `Organization`, `ContactPoint`,
  `WebPage`, `WebSite` (site boilerplate, no listing-specific schema).
- **houseboatsofseattle.com** — `https://houseboatsofseattle.com/wa/seattle/eastlake/` (no
  dedicated Riva/#503 page; general Eastlake neighborhood page) → 200. `<title>`: `Eastlake -
  Houseboats of Seattle (And Floating Homes!)`. No meta description tag found. JSON-LD `@type`s
  present: `WebPage`, `ReadAction`, `ImageObject`, `BreadcrumbList`, `ListItem` (x4), `WebSite`,
  `SearchAction`, `EntryPoint`, `PropertyValueSpecification`. Dollar amounts on page ($50k-$90k+)
  are houseboat moorage/slip prices, not condo listing prices — not comparable to our unit.

## 4. Core Web Vitals / PageSpeed

**Status: unavailable this run.** Single attempt against the PageSpeed Insights v5 REST API
(`googleapis.com/pagespeedonline/v5/runPagespeed?...&strategy=mobile&category=performance`) →
HTTP `429`, `"Quota exceeded for quota metric 'Queries' and limit 'Queries per day'"`. No retry,
per policy. No mobile LCP/INP/CLS values obtained.

## 5. Action Queue verification (`ACTION-QUEUE.md` → "## Do now")

1. **"Add GA4 to Rela" (native Rela GA field, not the Custom Script workaround)** —
   **CANNOT VERIFY FROM PAGE.** The rendered page cannot distinguish whether a `gtag` snippet
   originates from Rela's native GA-ID field vs. the Custom Script field — both would emit
   identical markup. What *is* directly confirmed from the page: `G-95JF0FLSZH` is present and
   loading (2 occurrences), same as it has been since 2026-09-10 via the Custom Script field per
   `ACTION-QUEUE.md`'s own "Done" log. No new evidence either way on whether the native field
   itself has since been populated.
2. **"Extend the Custom-Script JSON-LD with a full `RealEstateAgent` node"** — **NOT DONE.** The
   only `RealEstateAgent` node on the page is still the minimal `provider` object on the Block 1
   `Apartment` node (`name` + `email` only — no `phone`, no brokerage `name`, no headshot
   `image`). The Block 2 `ApartmentComplex` node has no `RealEstateAgent` reference at all, and
   there is no `@graph` combining the two. Unchanged from prior runs.

## 6. Deltas vs prior record

`state/last-snapshot.json` is still the unpopulated placeholder
(`{"schema_version":1,"first_run":null}`) — Tier 2 owns this file and it was not modified here.

**vs. the last successful same-day Tier 1 check-in (commit `f9b2605`, ~11:39 UTC):** No
substantive change detected anywhere on the live site — every field checked in §1 (title, meta
description, canonical, robots meta, og:*/twitter:* tags, both JSON-LD blocks verbatim, H1 text,
image/alt/lazy-load counts, GA tag IDs, `/llms.txt`, `/sitemap.xml`, `/robots.txt`) is
value-for-value identical to that run. Search-snapshot results are also consistent: zero
appearance of `eastlakeview503.relahq.com` across all 5 tracked queries and the indexation
`site:` query. Competitor deep-fetch results (rsir.com block, windermere.com, compass.com,
seattlecondosandlofts.com, houseboatsofseattle.com) are unchanged in structure; windermere.com's
visible list price (`$385,000`) is recorded for the first time in this level of detail but is
for a different unit (#506) and is not a comp-price change for our unit. PageSpeed remains
unavailable via the REST API (429 both times observed today).

**vs. `README.md`'s 2026-09-10 baseline / pending-fix list:** unchanged — restated for
continuity:

| Item | 2026-09-10 baseline / pending status | Observed this run (2026-09-11) |
|---|---|---|
| `twitter:card` | pending (absent) | **still absent** |
| `og:url` | pending (absent) | **still absent** |
| `og:locale` | pending (absent) | **still absent** |
| `twitter:image:alt` | listed as pending fix | **present**, descriptive alt text |
| H1 target text `1550 Eastlake Ave E #503, Seattle, WA 98102` | pending | H1 **exists** but reads `1550 Eastlake Ave E Apt 503, Seattle` — "Apt 503" not "#503", omits ", WA 98102" |
| JSON-LD `numberOfBedrooms` | pending (was `numberOfRooms`) | **still `numberOfRooms`** |
| JSON-LD `amenityFeature` | pending on the main listing node | **still absent on `Apartment` node**; present only on the separate `ApartmentComplex` node, not linked via `containedInPlace` |
| JSON-LD `petsAllowed` | pending | **still absent** (both nodes) |
| JSON-LD `containedInPlace` | pending | **still absent** |
| `<img loading="lazy">` | pending | **still 0 occurrences** |
| `/sitemap.xml` = 200 | pending | **still 404** |
| `robots.txt` without `Crawl-delay: 10` + with `Sitemap:` line | pending | **still has `Crawl-delay: 10`, still no `Sitemap:` line** |
| `/llms.txt` populated | pending | **still not populated** (SPA fallback) |
| GA4 custom tag (`G-95JF0FLSZH`) | claimed live 2026-09-10 | **confirmed live** |
| Title / meta description / og:title / JSON-LD `yearBuilt` (1997) / `offers.price` (649888) | 2026-09-10 baseline | **unchanged** |

Standing observation (unchanged): of the 19 property-photo `<img>` tags on the page, **none
carry alt text**.
